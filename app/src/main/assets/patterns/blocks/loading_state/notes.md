Use while fetching remote data or running a long task. Use the system
large ProgressBar style — CircularProgressIndicator from Material is
bundled but its attribute set is verbose. This pattern keeps compile
risk minimal.
