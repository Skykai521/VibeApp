For empty/loading/error states, inflate a block pattern at runtime and swap
visibility with the RecyclerView. The RecyclerView item layout should be
`list_item_two_line` or similar. Wire the FAB click to launch whatever
"create new" flow your app needs.
