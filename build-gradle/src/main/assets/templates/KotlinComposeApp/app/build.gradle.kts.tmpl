plugins {
    alias(libs.plugins.android.application)
    // AGP 9.x has built-in Kotlin support; no separate kotlin.android plugin.
    alias(libs.plugins.kotlin.compose)
    // Tencent Shadow plugin. Adds a "Shadow" flavor dimension with two
    // flavors (normal / plugin); only the plugin flavor gets Shadow's
    // bytecode transform. Use `:app:assemblePluginDebug` to produce the
    // Shadow-loadable APK.
    alias(libs.plugins.shadow)
}

shadow {
    transformConfig {
        // Classes that should keep resolving to the host's copies rather
        // than the plugin's — Shadow won't rewrite references to these.
        // Extend if the user's plugin needs to pass specific types across
        // the host/plugin boundary (e.g., shared AIDL interfaces).
        useHostContext = arrayOf(
            "com.vibe.app.plugin.IPluginInspector",
        )
    }
}

android {
    namespace = "{{PACKAGE_NAME}}"
    compileSdk = 34

    defaultConfig {
        applicationId = "{{PACKAGE_NAME}}"
        minSdk = 24
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        debug {
            isMinifyEnabled = false
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    buildFeatures {
        compose = true
    }
}

kotlin {
    compilerOptions {
        jvmTarget.set(org.jetbrains.kotlin.gradle.dsl.JvmTarget.JVM_17)
    }
}

dependencies {
    implementation(libs.androidx.core.ktx)
    implementation(libs.androidx.activity.compose)
    implementation(libs.androidx.lifecycle.runtime.ktx)
    implementation(libs.androidx.lifecycle.viewmodel)
    implementation(libs.androidx.savedstate)
    implementation(platform(libs.compose.bom))
    implementation(libs.compose.ui)
    implementation(libs.compose.ui.graphics)
    implementation(libs.compose.material3)
}
